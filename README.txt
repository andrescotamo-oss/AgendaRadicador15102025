# AGENDA ↔ RADICADOR (Streamlit)

## Desarrollo
1) pip install -r requirements.txt
2) streamlit run app.py

## EXE (Windows, todo incluido)
1) pip install -r requirements-freeze.txt
2) pip install pyinstaller
3) pyinstaller --clean build.spec
4) Ejecutable: dist/AgendaRadicador.exe
